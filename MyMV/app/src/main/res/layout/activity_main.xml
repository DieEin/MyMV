<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.elsyscoursework.mymv.MainActivity">

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vehicle_list" />

    <Button
        android:layout_width="@dimen/add_vehicle_button_dimensions"
        android:layout_height="@dimen/add_vehicle_button_dimensions"
        android:gravity="center_vertical|center_horizontal"
        android:id="@+id/add_vehicle_button"
        android:layout_below="@+id/vehicle_list"
        android:background="@drawable/round_button"
        android:text="@string/add_new"/>

    <Button
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:id="@+id/test"
        android:layout_below="@+id/add_vehicle_button"
        android:text="test"/>

</RelativeLayout>
